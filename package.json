{
  "name": "cafedebug-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Cafe Debug platform monorepo foundation",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "ci:admin:build": "pnpm --filter @cafedebug/admin run build",
    "ci:admin:test": "pnpm --filter @cafedebug/admin run test",
    "ci:admin:validate": "pnpm --filter @cafedebug/admin run lint && pnpm --filter @cafedebug/admin run typecheck",
    "ci:validation": "pnpm run ci:admin:build && pnpm run ci:admin:test && pnpm run ci:admin:validate",
    "ci:web:build": "pnpm --filter @cafedebug/web run build",
    "ci:web:test": "pnpm --filter @cafedebug/web run test",
    "ci:web:validate": "pnpm --filter @cafedebug/web run lint && pnpm --filter @cafedebug/web run typecheck",
    "ci:web:validation": "pnpm run ci:web:build && pnpm run ci:web:test && pnpm run ci:web:validate",
    "ci:api-client:build": "pnpm --filter @cafedebug/api-client run build",
    "ci:api-client:test": "pnpm --filter @cafedebug/api-client run test",
    "ci:api-client:validate": "pnpm --filter @cafedebug/api-client run lint && pnpm --filter @cafedebug/api-client run typecheck",
    "ci:api-client:validation": "pnpm run ci:api-client:build && pnpm run ci:api-client:test && pnpm run ci:api-client:validate",
    "clean": "turbo run clean",
    "docker:admin:config": "docker compose -f infra/docker/docker-compose.admin.yml --env-file .env config",
    "docker:admin:dev": "docker compose -f infra/docker/docker-compose.admin.yml --env-file .env up --build admin",
    "docker:admin:down": "docker compose -f infra/docker/docker-compose.admin.yml --env-file .env down --remove-orphans",
    "docker:admin:build": "docker build -f infra/docker/admin/Dockerfile --target production -t cafedebug/admin:local ."
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
