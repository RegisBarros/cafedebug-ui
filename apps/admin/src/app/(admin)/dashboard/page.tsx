import { ComingSoonPage } from "@/features/placeholders/coming-soon-page";

export default function DashboardPage() {
  return (
    <ComingSoonPage
      route="/dashboard"
      summary="Dashboard analytics are intentionally disabled for V1 and will be enabled once summary endpoints and metrics widgets are finalized."
      title="Dashboard"
    />
  );
}
