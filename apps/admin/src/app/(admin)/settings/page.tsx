import { ComingSoonPage } from "@/features/placeholders/coming-soon-page";

export default function SettingsPage() {
  return (
    <ComingSoonPage
      route="/settings"
      summary="Settings is a deliberate V1 placeholder. Profile and SEO settings forms will be introduced in a later milestone."
      title="Settings"
    />
  );
}
